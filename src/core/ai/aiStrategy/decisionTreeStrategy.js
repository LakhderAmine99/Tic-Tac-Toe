import AIStrategy from "./AIStrategy.js";

export default class DecisionTreeStrategy extends AIStrategy {

    constructor(){}

    /**
     * 
     * @override
     * @returns 
     */
    predictNextMove(){
        return;
    }
}